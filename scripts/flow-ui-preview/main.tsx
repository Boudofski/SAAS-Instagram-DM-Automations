// Isolated fixture with sample data; server actions are replaced by build-time mocks.
import React, {useState} from 'react';import {createRoot} from 'react-dom/client';
import AutomationTypePicker from '@/components/automations/automation-type-picker';import FlowBuilder from '@/components/automations/flow-builder';
function App(){const [dark,setDark]=useState(true);const [mobile,setMobile]=useState(false);const query=new URLSearchParams(window.location.search);const type=query.get('type');return <div className={dark?'dark':''}><div className="min-h-screen bg-slate-100 text-slate-950 dark:bg-[#080b13] dark:text-white"><header className="flex items-center justify-between p-4"><strong>AP3K · isolated design preview</strong><div className="flex gap-4"><button onClick={()=>setMobile(!mobile)}>{mobile?'Desktop':'Mobile'}</button><button onClick={()=>setDark(!dark)}>{dark?'Light mode':'Dark mode'}</button><a href={window.location.pathname}>Templates</a></div></header><div style={mobile?{width:390,maxWidth:'100%'}:{}} className="mx-auto p-4">{type?<FlowBuilder slug="sample" integrationId="12345678-1234-1234-1234-123456789012" templateId={query.get('template')??undefined} plan="PRO" refreshPosts={()=>{}}/>:<AutomationTypePicker slug="sample"/>}</div></div></div>};createRoot(document.getElementById('root')!).render(<App/>);
